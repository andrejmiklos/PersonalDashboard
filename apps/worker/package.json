{
  "name": "@dashboard/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').cpSync('public', '../../dist/public', { recursive: true })\"",
    "typecheck": "tsc -p . && tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260923.1",
    "@types/node": "^22.20.4"
  }
}
